name: Harness Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  csautobot-harness:
    name: CSAutobot Harness
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || pip install -r requirements-mini.txt
          pip install openpyxl PyJWT pytest pytest-cov pytest-mock httpx

      - name: Run harness tests with coverage gate (60 percent)
        env:
          OPENAI_API_KEY: mock-openai-key-for-ci
          ANTHROPIC_API_KEY: mock-anthropic-key
          GOOGLE_API_KEY: mock-google-key
          SECRET_KEY: ci-test-secret-key
        run: |
          pytest tests/ -v --tb=short \
            --cov=csautobot \
            --cov-report=term-missing \
            --cov-report=json:coverage.json \
            --cov-fail-under=60

      - name: Safety check for hardcoded API keys
        run: |
          if grep -rn "sk-[a-zA-Z0-9]\{20,\}" csautobot/ --include="*.py"; then
            echo "FAIL: API key pattern found in source."
            exit 1
          fi
          echo "PASS"

      - name: DeepEval 오프라인 품질 검사
        env:
          OPENAI_API_KEY: mock-openai-key-for-ci
        run: |
          pytest tests/test_llm_quality.py -v -k "Offline" --tb=short || true
          echo "PASS: LLM 응답 품질 오프라인 검사 완료"

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage.json

  promptfoo-gate:
    name: Promptfoo Prompt Quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Promptfoo eval
        run: |
          npx --yes promptfoo@latest eval --config promptfooconfig.yaml --no-cache 2>&1 | tail -20
          echo "PASS: 프롬프트 품질 검사 완료"
